## Packages
framer-motion | Page transitions and complex animations
clsx | Conditional class names
tailwind-merge | Merging tailwind classes intelligently
date-fns | Date formatting for orders

## Notes
Tailwind Config - extend colors:
primary: #FF7A00 (Orange)
secondary: #000000 (Black)
background: #FFFFFF (White)

Auth uses Replit Auth (via /api/login, /api/logout).
Images use placeholder services for now.
